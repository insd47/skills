{
  "mcpServers": {
    "codex-app": {
      "type": "stdio",
      "command": "${CLAUDE_PLUGIN_ROOT}/bin/codex-app",
      "args": ["server"],
      "cwd": "${CLAUDE_PROJECT_DIR}",
      "alwaysLoad": true,
      "timeout": 3600000
    }
  }
}
